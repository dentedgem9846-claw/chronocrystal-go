# ChronoCrystal Configuration
# The time dragon's lair configuration file

[agent]
# Ollama model to use
model = "llama3.2"
# Maximum tool iterations per order
max_tool_iterations = 20
# Tool execution timeout in seconds
tool_timeout = 60
# Maximum context window in tokens
context_window = 8192
# Number of recent messages to never prune
recent_messages_keep = 10
# Custom system prompt (appended to default)
# system_prompt = ""

[provider]
# Ollama server URL
url = "http://localhost:11434"
# Request timeout in seconds
timeout = 120
# Circuit breaker settings
circuit_threshold = 3      # consecutive failures before opening
circuit_cooldown = "30s"  # time before half-open state

[channel]
# Path to simplex-chat binary
simplex_path = "simplex-chat"
# Database file for SimpleX
db_path = "simplex.db"
# Auto-accept new contacts
auto_accept = true
# Reconnection settings
max_retries = 20         # give up after this many consecutive failures
initial_backoff = "1s"  # first retry delay
max_backoff = "30s"     # maximum retry delay
backoff_factor = 2.0    # delay multiplier between retries

[memory]
# Path to DoltLite database file
db_path = "chronocrystal.db"
# Whether to dolt_commit after every state change
auto_commit = true

[logging]
# Log level: debug, info, warn, error
level = "info"
# Log file path (empty = stdout)
# file = ""

[tools]
# Directory containing tool programs
dir = "./tools"
# Pre-compile tools on startup (faster execution, slower startup)
precompile = false